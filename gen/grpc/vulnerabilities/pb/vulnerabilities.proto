// Code generated with goa v3.2.6, DO NOT EDIT.
//
// vulnerabilities protocol buffer definition
//
// Command:
// $ goa gen github.com/brittonhayes/citadel/design

syntax = "proto3";

package vulnerabilities;

option go_package = "vulnerabilitiespb";

// The vulnerability service handles the creation and querying of security
// vulnerabilities.
service Vulnerabilities {
	// Find implements find.
	rpc Find (FindRequest) returns (FindResponse);
	// List all of the vulnerabilities
	rpc List (ListRequest) returns (ListResponse);
}

message FindRequest {
	// Unique ID of the vulnerability
	uint64 id = 1;
}

message FindResponse {
	// Unique ID of the vulnerability
	uint64 id = 1;
	// Title of the vulnerability
	string title = 2;
	// Description of the vulnerability
	string description = 3;
	// If the vulnerability is exploitable
	bool exploitable = 4;
	// Severity score of the vulnerability
	float cvss_score = 5;
	// If the vulnerability is patchable
	bool is_patchable = 6;
	// If the vulnerability is upgradeable
	bool is_upgradeable = 7;
}

message ListRequest {
	// Limit the number of results
	sint32 limit = 1;
}

message ListResponse {
	repeated Vulnerability field = 1;
}

message Vulnerability {
	// Unique ID of the vulnerability
	uint64 id = 1;
	// Title of the vulnerability
	string title = 2;
	// Description of the vulnerability
	string description = 3;
	// If the vulnerability is exploitable
	bool exploitable = 4;
	// Severity score of the vulnerability
	float cvss_score = 5;
	// If the vulnerability is patchable
	bool is_patchable = 6;
	// If the vulnerability is upgradeable
	bool is_upgradeable = 7;
}
